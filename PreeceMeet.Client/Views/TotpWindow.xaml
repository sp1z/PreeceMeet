<Window x:Class="PreeceMeet.Views.TotpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PreeceMeet - Two-Factor Authentication"
        Width="380" Height="280"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#0f0f1a"
        Foreground="White">
    <Window.Resources>
        <Style x:Key="DarkInput" TargetType="TextBox">
            <Setter Property="Background" Value="#1e1e30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3a5c"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4f46e5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4338ca"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#2d2a5a"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="40,30">
        <TextBlock Text="Two-Factor Authentication" FontSize="18" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,6"/>
        <TextBlock Text="Enter the 6-digit code from your authenticator app."
                   FontSize="12" Foreground="#888" TextWrapping="Wrap"
                   HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,24"/>

        <TextBox x:Name="TxtCode" Style="{StaticResource DarkInput}" MaxLength="6"
                 Margin="0,0,0,6" KeyDown="TxtCode_KeyDown" TextChanged="TxtCode_TextChanged"/>

        <TextBlock x:Name="TxtError" Foreground="#f87171" FontSize="12"
                   Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <Button x:Name="BtnVerify" Content="Verify" Style="{StaticResource PrimaryButton}" Click="BtnVerify_Click"/>
    </StackPanel>
</Window>
