<Window x:Class="PreeceMeet.Views.TotpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PreeceMeet — Two-Factor Authentication"
        Width="320" Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display">

    <Grid Margin="24,20,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Two-Factor Authentication"
                   FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="1" Text="Enter the 6-digit code from your authenticator app."
                   FontSize="12" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,16"/>

        <TextBox Grid.Row="2" x:Name="TxtCode" MaxLength="6" FontSize="24"
                 TextAlignment="Center" Margin="0,0,0,8"
                 KeyDown="TxtCode_KeyDown" TextChanged="TxtCode_TextChanged"/>

        <TextBlock Grid.Row="3" x:Name="TxtError" Foreground="Red" FontSize="12"
                   Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,0,0,8"/>

        <Button Grid.Row="4" x:Name="BtnVerify" Content="Verify" IsDefault="True"
                HorizontalAlignment="Stretch" Padding="0,6"
                Click="BtnVerify_Click"/>
    </Grid>
</Window>
