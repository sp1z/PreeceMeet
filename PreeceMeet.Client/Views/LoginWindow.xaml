<Window x:Class="PreeceMeet.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PreeceMeet - Sign In"
        Width="420" Height="380"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#0f0f1a"
        Foreground="White">
    <Window.Resources>
        <Style x:Key="DarkInput" TargetType="TextBox">
            <Setter Property="Background" Value="#1e1e30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3a5c"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style x:Key="DarkPassword" TargetType="PasswordBox">
            <Setter Property="Background" Value="#1e1e30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3a5c"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4f46e5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4338ca"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#2d2a5a"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="40,30">
        <TextBlock Text="PreeceMeet" FontSize="28" FontWeight="Bold" Foreground="#7c6fe0"
                   HorizontalAlignment="Center" Margin="0,0,0,4"/>
        <TextBlock Text="Sign in to continue" FontSize="13" Foreground="#888"
                   HorizontalAlignment="Center" Margin="0,0,0,28"/>

        <TextBlock Text="Email" FontSize="12" Foreground="#bbb" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtEmail" Style="{StaticResource DarkInput}" Margin="0,0,0,14" KeyDown="Input_KeyDown"/>

        <TextBlock Text="Password" FontSize="12" Foreground="#bbb" Margin="0,0,0,4"/>
        <PasswordBox x:Name="TxtPassword" Style="{StaticResource DarkPassword}" Margin="0,0,0,6" KeyDown="Input_KeyDown"/>

        <CheckBox x:Name="ChkRemember" Content="Remember me" Foreground="#bbb" Margin="0,0,0,20"/>

        <TextBlock x:Name="TxtError" Foreground="#f87171" FontSize="12"
                   Visibility="Collapsed" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <Button x:Name="BtnLogin" Content="Sign In" Style="{StaticResource PrimaryButton}" Click="BtnLogin_Click"/>
    </StackPanel>
</Window>
