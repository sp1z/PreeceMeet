<Window x:Class="PreeceMeet.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PreeceMeet — Sign In"
        Width="380" Height="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White">
        <Grid>
            <!-- Header band -->
            <Border Background="#0078D4" VerticalAlignment="Top" Height="72">
                <StackPanel VerticalAlignment="Center" Margin="24,0">
                    <TextBlock Text="PreeceMeet" FontSize="22" FontWeight="SemiBold"
                               Foreground="White"/>
                    <TextBlock Text="Sign in to your account" FontSize="12"
                               Foreground="#CCE4F7" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Form -->
            <Grid Margin="24,88,24,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Email address" Style="{StaticResource FieldLabel}"/>
                <TextBox Grid.Row="1" x:Name="TxtEmail"
                         Padding="6,5" FontSize="13" Margin="0,0,0,12"
                         KeyDown="Input_KeyDown"/>

                <TextBlock Grid.Row="2" Text="Password" Style="{StaticResource FieldLabel}"/>
                <PasswordBox Grid.Row="3" x:Name="TxtPassword"
                             Padding="6,5" FontSize="13" Margin="0,0,0,8"
                             KeyDown="Input_KeyDown"/>

                <CheckBox Grid.Row="4" x:Name="ChkRemember" Content="Remember my email"
                          FontSize="12" Margin="0,0,0,6"/>

                <TextBlock Grid.Row="5" x:Name="TxtError" Foreground="#C42B1C" FontSize="12"
                           Visibility="Collapsed" TextWrapping="Wrap" Margin="0,0,0,4"
                           VerticalAlignment="Bottom"/>

                <Button Grid.Row="6" x:Name="BtnLogin" Content="Sign In" IsDefault="True"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource AccentButton}"
                        Click="BtnLogin_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
