<Window x:Class="PreeceMeet.Views.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="380" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display">

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" x:Name="LblEmail" Content="Email" Padding="0,0,0,2"/>
        <TextBox Grid.Row="1" x:Name="TxtEmail" Margin="0,0,0,12"/>

        <Label Grid.Row="2" Content="Password" Padding="0,0,0,2"/>
        <PasswordBox Grid.Row="3" x:Name="TxtPassword" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Padding="16,5" Margin="0,0,8,0"
                    Click="BtnCancel_Click"/>
            <Button Content="OK" IsDefault="True" Padding="16,5"
                    Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>
