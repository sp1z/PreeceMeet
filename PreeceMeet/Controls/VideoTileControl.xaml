<UserControl x:Class="PreeceMeet.Controls.VideoTileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="270" d:DesignWidth="480">
    <Grid Background="#1a1a2e">
        <!-- Video surface – replaced at runtime by a D3DImage/WriteableBitmap host -->
        <Image x:Name="PART_VideoImage"
               Stretch="Uniform"
               RenderOptions.BitmapScalingMode="HighQuality"/>

        <!-- Fallback avatar shown when no video track is active -->
        <Grid x:Name="PART_AvatarOverlay" Visibility="Visible">
            <Ellipse Width="72" Height="72"
                     Fill="#2d2d44"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"/>
            <TextBlock x:Name="PART_AvatarInitial"
                       Text="?"
                       FontSize="30"
                       Foreground="#a0a0c0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Name badge at the bottom -->
        <Border VerticalAlignment="Bottom"
                HorizontalAlignment="Left"
                Margin="6" Padding="6,3"
                Background="#88000000"
                CornerRadius="4">
            <StackPanel Orientation="Horizontal">
                <!-- Muted microphone indicator -->
                <TextBlock x:Name="PART_MutedIcon"
                           Text="🔇"
                           FontSize="12"
                           Margin="0,0,4,0"
                           Visibility="Collapsed"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="PART_NameLabel"
                           Text="Participant"
                           Foreground="White"
                           FontSize="12"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
